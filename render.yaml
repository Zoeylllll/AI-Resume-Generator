services:
  - type: web
    name: ai-resume-generator
    env: python
    buildCommand: ""
    startCommand: streamlit run app.py --server.port=10000
    envVars:
      - key: OPENAI_API_KEY
        value: your_real_openai_api_key_here
